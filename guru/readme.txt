mobile app for health tracking app and personalised planner 
uses gen-ai for the planner as well as the chatbot 
django & django rest framework (DRF) for backend and API
react native + bootstrap for frontend 
postgresql / mysql for database 
celery/redis (optional) for async tasks like ai-generated plans 
firebase for authentication 
docker for deployment 
fastapi (optional) for separate ai serving backend